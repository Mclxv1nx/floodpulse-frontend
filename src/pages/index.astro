---
import Layout from '@/layouts/Layout.astro';
import Dashboard from '@/components/Dashboard';
---
<Layout>
  {/* client:only — Leaflet necesita `window`, no se puede renderizar en el servidor */}
  <Dashboard client:only="react" />
</Layout>
